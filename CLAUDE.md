# [Claude Code 작업 지시서] 빅데이터 기반 장비 모델별 구매 품목 및 협력업체 분석 대시보드

- 기획자: 정하진 (Hajin jung)
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] Padlet → "프로젝트 기획서" 섹션
- 프로젝트 유형: 데이터 분석 + 대시보드 시각화 + 보고서 생성

---

## 0. Claude Code에게 — 작업 방식

이 문서는 한 명의 수강생이 작성한 프로젝트 기획서를 바탕으로 작성된 개발 지시서입니다. 아래 순서로 진행하세요.

1. 이 문서를 끝까지 읽고, **9번 "확인 필요 사항"** 중 작업에 꼭 필요한 항목만 사용자에게 먼저 질문한다. 답이 없으면 **5번 "가정"**을 적용해 진행한다.
2. 실제 회사 데이터는 제공되지 않는다. **4번의 스키마대로 샘플 엑셀 2개를 먼저 생성**하고 그 데이터로 전체 파이프라인이 동작하는 것을 보여준다.
3. 단계(Phase)별로 커밋하고, 각 단계 종료 시 실행 방법과 결과 화면(스크린샷 또는 설명)을 보고한다.
4. 부록의 기획서 원문을 최종 기준으로 삼는다. 본문 정리와 원문이 충돌하면 원문을 따른다.

---

## 1. 한 줄 요약

구매 데이터(품번·업체·물량·금액)와 장비 모델 데이터(품번·모델·톤급)를 **품번으로 조인**한 뒤, **톤급 → 장비 모델 → 품번 → 협력업체** 계층으로 드릴다운하며 구매 물량·금액·업체별 비중(%)을 볼 수 있는 대시보드와 요약 보고서(PDF/PPT)를 만든다.

## 2. 배경 및 현재 업무 상황

- 현재는 단순 엑셀 구매 데이터만 존재하며, 장비 톤급·모델별로 구매 현황과 업체 공급 비중을 한눈에 보기 어렵다.
- 목표는 "어느 톤급·모델에서 어떤 협력업체의 구매 비중이 높은가"를 직관적으로 파악하는 것 (업체 집중도·의존도 분석).
- 결과물 형식은 무관 (웹 대시보드, PPT, PDF 모두 허용).

## 3. 요구 기능

### 3.1 데이터 처리
- 엑셀 2개 업로드 → 정제 → 품번(Part Number) 기준 조인.
- 하나의 품번이 여러 장비 모델에 적용되는 경우(1:N) 모든 모델에 연계하여 분석.
- 장비 모델의 톤급 코드를 톤급으로 변환해 그룹화:
  - `2X → 2톤급`, `3X → 3톤급`, … `8X → 8톤급` (앞자리 숫자 + X)
  - `100 → 10톤급`, `120 → 12톤급`, `140 → 14톤급`, `160 → 16톤급`, `180 → 18톤급` (세 자리 수는 ÷10)
  - 정렬은 2톤 → 3톤 → … → 18톤 순 (숫자 순, 문자열 정렬 금지).

### 3.2 분석 지표 (톤급 / 모델 / 품번 / 업체 각 레벨)
- 구매 금액 합계
- 구매 물량 합계
- 업체별 구매 물량·금액 및 **비중(%)** — 해당 레벨 합계 대비
- 특정 업체 집중도(예: 1위 업체 비중이 N% 이상인 모델/품번 하이라이트)

### 3.3 대시보드
- 세로축(계층): 톤급 → 장비 모델 → 품번 → 협력업체 드릴다운(트리/아코디언 또는 클릭 네비게이션).
- 각 노드에 물량·금액·업체 비중 차트(막대/도넛 등).
- 필터: 톤급, 모델, 업체.
- 예시 표현:
  ```
  2톤급 (2X)
    모델 A : A업체 45% / B업체 30% / C업체 25%
    모델 B : A업체 20% / D업체 50% / E업체 30%
  3톤급 (3X)
    모델 C : A업체 35% / B업체 40% / D업체 25%
  ```

### 3.4 보고서
- 주요 분석 결과를 문서(PDF 또는 PPT)로 내보내기: 톤급별 구매 규모, 모델별 업체 공급 비중, 업체 집중도 상위 항목.

## 4. 데이터 스키마 (샘플 데이터 생성 지침)

**Data 1. 구매 데이터 (`purchase.xlsx`)**

| 컬럼 | 설명 | 예시 |
|---|---|---|
| 품번 (Part Number) | 키 | P-10023 |
| 업체명 | 협력업체 | A업체 |
| 연간 구매 물량 | 수량 | 1,200 |
| 연간 구매 금액 | 원 | 36,000,000 |

**Data 2. 장비 모델 데이터 (`model.xlsx`)**

| 컬럼 | 설명 | 예시 |
|---|---|---|
| 품번 (Part Number) | 키 (Data 1과 조인) | P-10023 |
| 장비 모델명 | 적용 모델 | 25D-9 |
| 톤급 코드 | 2X / 3X / … / 100 / 120 / … / 180 | 2X |

샘플 생성 시: 품번 300개 이상, 업체 8~10개, 톤급 2X~180 고르게, 1:N 품번 일부 포함, 동일 품번 복수 업체 공급 일부 포함.

## 5. 제약사항 및 가정

- 실제 데이터는 사내 자료이므로 제공되지 않음 → 샘플 데이터로 개발, 실제 엑셀을 업로드하면 그대로 동작해야 함 (컬럼명 매핑 UI 또는 설정 파일 제공).
- 브라우저만으로 실행 가능한 형태를 우선(수강생 개인 노트북 환경). 서버가 필요하면 로컬 실행 한 줄 명령으로.
- 한글 컬럼명·한글 업체명 정상 처리 (인코딩 주의).

## 6. 권장 기술 스택 (제안 — 사용자 확인 후 변경 가능)

- 옵션 A (권장): 단일 HTML + JavaScript (SheetJS로 엑셀 파싱, Chart.js/ECharts 시각화) → GitHub Pages 배포 가능.
- 옵션 B: Python (pandas + Streamlit/Plotly) → PDF/PPT 내보내기 용이 (python-pptx, reportlab).
- 보고서 내보내기는 옵션 A의 경우 브라우저 인쇄(PDF) 또는 html2pdf, PPT는 PptxGenJS.

## 7. 산출물

1. 샘플 엑셀 2개
2. 대시보드 (웹 또는 앱)
3. 요약 보고서 생성 기능 (PDF/PPT)
4. README: 실행 방법, 엑셀 컬럼 요구 형식, 톤급 코드 변환 규칙

## 8. 구현 단계

- Phase 1: 샘플 데이터 생성 + 조인/톤급 변환/집계 로직 + 콘솔 또는 표 출력으로 검증
- Phase 2: 드릴다운 대시보드 UI + 차트 + 필터
- Phase 3: 업체 집중도 하이라이트 + 보고서(PDF/PPT) 내보내기
- Phase 4: 실제 엑셀 컬럼 매핑 UI, README, 배포

## 9. 확인 필요 사항 (작업 전 질문)

1. 대시보드 형태 선호: 웹(HTML) / Streamlit / 엑셀+PPT만?
2. 실제 엑셀의 정확한 컬럼명과 시트 구조(헤더 행 위치)?
3. 톤급 코드의 전체 목록(2X~8X, 100~180 외 다른 코드 존재 여부, 예: 9X, 200, 250?)
4. 업체 집중도 "경고" 기준 비중(예: 50% 이상)?
5. 보고서는 PDF와 PPT 중 어느 것이 우선인가?

---

## 부록. 기획서 원문 (Padlet 게시 내용 그대로)

프로젝트명
빅데이터 기반 장비 모델별 구매 품목 및 협력업체 분석 대시보드 구축

1. 프로젝트 목적
구매 관련 빅데이터를 활용하여 장비 모델별 구매 품목, 협력업체, 구매 물량 및 구매 금액을 분석하고, 이를 대시보드 형태로 시각화하는 것을 목표로 합니다. 단, 형식은 무관합니다.
기존의 단순 Excel 데이터의 구매 데이터를 장비 톤급 및 모델별로 그룹화하여 구매 현황과 업체별 공급 비중을 직관적으로 파악할 수 있는 시각화 자료를 구축하고자 합니다.

2. 활용 데이터 (엑셀 데이터 2개)
Data 1. 구매 데이터: 품번(Part Number), 업체명, 품번별 연간 구매 물량, 품번별 연간 구매 금액
Data 2. 장비 모델 데이터: 품번(Part Number), 해당 품번이 적용되는 장비 모델명, 장비 톤급 정보
두 데이터를 공통 항목인 품번(Part Number)을 기준으로 연결하여 통합 분석 데이터를 구성합니다.
※ 하나의 품번이 여러 장비 모델에 적용되는 경우 해당 모델 정보를 연계하여 분석합니다.

3. 데이터 분류
장비 모델을 톤급 코드 기준으로 그룹화하여 분석합니다.
예시) 2X → 2톤급, 3X → 3톤급, 4X → 4톤급, 5X → 5톤급, 6X → 6톤급, 7X → 7톤급, 8X → 8톤급, ··· 100 → 10톤급, 120 → 12톤급, 140 → 14톤급, 160 → 16톤급, 180 → 18톤급
즉, 2톤, 3톤, 4톤 … 18톤 등 실제 장비 톤급을 기준으로 직접 그룹핑합니다.
각 톤급 안에서 업체 기준 데이터(물량/연간 금액/톤수내 업체별 비중)를 구분하여 분석합니다.

4. 분석 및 시각화 방법
세로축 : 장비 톤급별 / 모델별 그룹핑
2톤 → 3톤 → 4톤 → 5톤 → … → 18톤 순으로 구성하고, 각 톤급 안에서 해당 장비 모델별 구매 데이터를 분석합니다.
각 모델별로 다음 정보를 시각화합니다: 톤수별 구매 금액, 톤수별 구매 물량, 톤수별 업체별 구매 물량 및 비중(%)
예시)
2톤급 (2X) — 모델 A: A업체 45%, B업체 30%, C업체 25% / 모델 B: A업체 20%, D업체 50%, E업체 30%
3톤급 (3X) — 모델 C: A업체 35%, B업체 40%, D업체 25%
이와 같은 방식으로 톤급 → 협력업체 순으로 데이터를 구성하여 모델별 협력업체 분포와 구매 비중을 한눈에 확인할 수 있도록 합니다.

5. 최종 결과물
최종적으로 분석 결과를 대시보드 형태로 시각화하고, 주요 분석 결과를 문서(PDF 등) 형태로 표현하고자 합니다.
데이터 분석 및 결과물 작성 Process:
Raw Data → 데이터 정제 → 품번 기준 데이터 연결 → 톤급 코드 분류 → 톤급별 그룹핑 → 장비 모델별 분석 → 업체별 구매 금액 및 비중 분석 → 대시보드 시각화 → PPT 결과물
최종 대시보드는 다음과 같은 구조로 데이터를 확인할 수 있도록 구성합니다.
톤급(2X, 3X … 180) → 장비 모델 → 품번 → 협력업체 → 구매 물량/구매 금액/비중
이를 통해 대량의 구매 데이터를 단순 조회하는 수준을 넘어, 각 톤급에서 어떤 협력업체의 구매 비중이 높은지를 직관적으로 파악할 수 있는 분석 자료를 만드는 것이 프로젝트의 최종 목표입니다.

6. 기대 효과
톤급 및 장비 모델별 구매 구조와 협력업체 의존도를 시각적으로 확인할 수 있으며, 구매 금액 및 물량이 특정 업체에 집중되어 있는 품목이나 모델을 쉽게 파악할 수 있습니다.
향후에는 해당 데이터를 활용하여 톤급별 구매 규모 비교, 모델별 업체 공급 비중 분석, 특정 업체에 대한 구매 집중도 분석, 주요 협력업체 관리 및 구매 전략 수립 등에 활용할 수 있을 것으로 기대합니다.
